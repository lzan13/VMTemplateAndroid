<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_bg"
    android:orientation="vertical"
    tools:context=".ui.main.me.MeSignatureActivity"
    >
    <!--定义顶部 TopBar-->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        >
        <!--TopBar 顶部 statusBar 填充-->
        <View
            android:id="@+id/common_top_space"
            android:layout_width="match_parent"
            android:layout_height="@dimen/common_0"
            />
        <com.vmloft.develop.library.tools.widget.VMTopBar
            android:id="@+id/common_top_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/common_top_space"
            app:vm_top_bar_end_btn="@string/btn_finish"
            app:vm_top_bar_icon="@drawable/im_ic_arrow_left"
            app:vm_top_bar_title="@string/me_info"
            />
    </RelativeLayout>

    <EditText
        android:id="@+id/me_nickname_et"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/vm_margin_large"
        android:background="@drawable/common_input_rectangle_bg_shape"
        android:hint="@string/me_signature_hint"
        android:padding="@dimen/vm_padding_large"
        style="@style/VMEdit"
        />
</LinearLayout>